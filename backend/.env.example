# Database Configuration
# ---------------------
# Default: SQLite (for local development without Docker)
# DATABASE_URL=sqlite:///./data/eioku.db

# PostgreSQL (recommended for production and concurrent workloads)
# DATABASE_URL=postgresql://user:password@host:port/database
# Example for docker-compose:
DATABASE_URL=postgresql://eioku:eioku_dev@localhost:5432/eioku

# Test Database (optional - defaults to same as DATABASE_URL)
# TEST_DATABASE_URL=sqlite:///./test_data/test.db
